import { Routes } from '@angular/router';
import { CommandCenterComponent } from './features/command-center/command-center.component';

export const routes: Routes = [{ path: '', component: CommandCenterComponent }];
